import 'package:flutter/material.dart';
class MatchView extends StatelessWidget {
  const MatchView({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Match (Eşleşme) İçeriği", style: TextStyle(color: Colors.white)));
}